# 2026-04-04 — divebase Real-World Feedback Back-Ported

> A day of running the harness on a real project (divebase) and back-porting the gaps found into the template.

---

## Work Log

### 1. Added Project-Specific Placeholders to Skills (20:21)

- **bug-fix**: Added `{{VERIFY_CHECK_*}}`, `{{PITFALL_*}}` placeholders — customizable verification items and pitfalls per project
- **code-review**: Added `{{CRITICAL_CHECK_*}}`, `{{IMPORTANT_CHECK_*}}`, `{{PROJECT_CHECK_*}}` placeholders
- Added new placeholder descriptions and examples to PlaceholderGuide.md
- **Background**: While using skills on divebase, discovered that generic checklists were missing project-specific verifications

### 2. /review Command Commit Order Fix — divebase Back-Port (20:38)

- **Problem**: Yesterday (4/3), the Reviewer workflow commit order was fixed, but the `/review` command's own workflow numbers were not synced
- **Fix**: Aligned `.claude/commands/review.md` workflow order with role-reviewer.md
- Unified to handoff write -> commit order, completely resolving the issue of uncommitted files remaining after APPROVE

---

## Retrospective

**One-line summary**: Back-ported 2 issues discovered from divebase real-world application into the template.

**What went well**:

- Immediately executed yesterday's goal of "apply to a real project and collect feedback."
- The skill placeholder pattern was a good discovery — essentially punching holes that let generic checklists be extended per project.

**Lessons learned**:

- Template files (role-*.md) and command files (commands/*.md) have **workflow order duplicated in two places**. When you fix one, always check the other.
- Skill templates also need placeholders to be reusable across projects. The principle is to make everything that "could differ by project" into a hole.

**Next steps**:

- v4 upgrade work (BLOCK/WARN hooks, post-edit-test, scripts automation) — already prepared, waiting to commit
- Field-test automation scripts (run-task.sh, run-epic.sh)
