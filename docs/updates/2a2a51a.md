---
hash: 2a2a51a
date: 2026-04-23
severity: P1
type: feat
breaking: false
---

# [P1] feat: post-task handoff gate (role-reviewer + SessionEnd hook)

## Summary
APPROVE 이후 같은 세션에서 수행한 후속 작업(PR merge, 브랜치 정리, 상류 피드백 전달 등) 이 `handoff/latest.md` 에 반영 안 돼 다음 세션이 stale 진입하던 설계 gap 해결. Reviewer convention + SessionEnd 훅 조합.

## Commits
- `2a2a51a` feat: kody P1-4 — post-task handoff gate (role-reviewer + SessionEnd hook)

## Changes
- `src/.claude/hooks/handoff-freshness-check.sh` (신규) — SessionEnd 훅. HEAD 커밋 시각이 handoff mtime + 60s 보다 크면 경고 출력. 비차단 (exit 0), macOS/Linux stat 양쪽 지원
- `src/.claude/settings.json` — `SessionEnd` 블록에 훅 등록
- `src/templates/role-reviewer.md` — "## Post-task Activities" 섹션 추가 (`## Post-task activities` 서브섹션 append convention + SessionEnd 훅 언급)

## Manifest classification
- `[managed]` `.claude/hooks/**` — 훅 파일은 자동 전파 ✅
- `[managed]` `templates/**` — role-reviewer.md 자동 전파 ✅
- `[seed]` `.claude/settings.json` — **자동 전파 안 됨**. 각 다운스트림에서 수동으로 SessionEnd 블록 추가 필요

## Why
kody Epic 7 addendum E10/P1-4. Slice 7.15 APPROVE 후 PR merge + 상류 피드백 전달 후 handoff 는 "APPROVE 직후" 상태로 박제. 다음 세션이 이미 끝난 작업을 재시도할 위험.

피드백 원문: `docs/downstream-feedback/kody-2026-04-23.md` §E10 / §P1-4.

## Downstream impact
- 영향 대상: APPROVE 후 같은 세션에서 tail 작업을 하는 모든 다운스트림
- 적용 후: 세션 종료 시 handoff 가 stale 하면 경고 출력 → Reviewer 가 `## Post-task activities` 섹션 append
- **수동 작업 필요**: `.claude/settings.json` 에 `SessionEnd` 블록 추가:
  ```json
  "SessionEnd": [
    {
      "hooks": [
        { "type": "command", "command": ".claude/hooks/handoff-freshness-check.sh" }
      ]
    }
  ]
  ```

## Verification
- 훅 smoke: 임시 repo 에서 `touch -t 202001010000 handoff/latest.md` 후 새 커밋 → SessionEnd 시 경고
- settings.json 에 SessionEnd 등록됐는지 `python3 -c "import json; print(json.load(open('.claude/settings.json'))['hooks'].get('SessionEnd'))"`

## Related
- 피드백: `docs/downstream-feedback/kody-2026-04-23.md` §E10
- forge handoff: PM-5
